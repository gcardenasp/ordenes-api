# Perfil para desarrollo: lee las variables del archivo .env y apunta al Oracle de docker-compose.
spring:
  config:
    import: optional:file:.env[.properties]
  datasource:
    url: ${DB_URL:jdbc:oracle:thin:@//localhost:1521/FREEPDB1}

seguridad:
  usuario-local: usuario.local
